import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Hito-1-Music',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
